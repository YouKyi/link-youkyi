{
  "name": "link-occiclean",
  "version": "1.0.0",
  "private": true,
  "description": "Page de liens (link in bio) d'OcciClean.",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./assets/tailwind.css --minify",
    "watch": "tailwindcss -i ./src/input.css -o ./assets/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
